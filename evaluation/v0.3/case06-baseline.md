# v0.3 Case06 Baseline
"倾向于建议去" with caveat: asks user to find moving cost first. Provides formula for calculation. Note: baseline gets the direction right but frames moving cost as prerequisite ("先把搬家成本弄清楚") rather than non-blocking variable. This is a mild F2 (VOI Miss in reverse — treating non-blocking as blocking).
